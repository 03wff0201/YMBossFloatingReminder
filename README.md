# YMBossFloatingReminder

圆梦三职业 Boss 悬浮提醒工具。

这是一个安卓悬浮窗提醒工具，只用于手动记录 Boss 刷新时间和提醒。

它不会自动点击游戏，不会自动跑图，不会自动打怪。

## GitHub Actions 打包

进入 Actions，运行 `Build Android APK`，完成后在 Artifacts 下载 `app-debug.apk`。
